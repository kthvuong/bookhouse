{
  "name": "bookhouse",
  "private": true,
  "description": "A personal, single-user reading tracker. Static site with one Vercel serverless function for cross-device sync.",
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  }
}
